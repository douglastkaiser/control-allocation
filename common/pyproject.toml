[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "common"
version = "0.1.0"
dependencies = ["matplotlib", "sympy", "ipython"]

[tool.setuptools.packages.find]
include = ["common*"]
